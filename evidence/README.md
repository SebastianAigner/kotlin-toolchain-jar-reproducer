# Checked-in verification evidence

The `0.12.0/` directory contains concise output from a verified run of
`reproduce.sh`. Full logs remain in the ignored `evidence/latest/` directory
because they include machine-local paths and verbose build output.

| Version | JAR hashes | Changed ZIP timestamps | Extracted payload | Docker layer |
| --- | --- | --- | --- | --- |
| 0.12.0 | different | 131/131 | byte-identical | different |
